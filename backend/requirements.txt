# --- Core API ---------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # multipart form parsing for /voice-chat
websockets>=12.0               # /ws/alerts (not available on serverless hosts)
pydantic>=2.7

# --- Data + scheduling ------------------------------------------------------
httpx>=0.27                    # Open-Meteo client
APScheduler>=3.10              # the single alert scheduler

# --- AI ---------------------------------------------------------------------
anthropic>=1.0                 # Claude (structured tool use); optional at runtime

# --- Voice output -----------------------------------------------------------
gTTS>=2.5                      # text to speech; optional at runtime

# --- Notifications ----------------------------------------------------------
twilio>=9.0                    # SMS alerts; falls back to console logging

# Speech recognition lives in requirements-voice.txt. faster-whisper pulls in
# CTranslate2 and onnxruntime, which are far too large for a serverless bundle
# and are not needed by any other code path — /voice-chat still works there via
# the browser's own speech recognition (sent as `client_transcript`).
#
#   pip install -r requirements-voice.txt
#
# Test dependencies are in requirements-dev.txt.
